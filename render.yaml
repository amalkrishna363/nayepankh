databases:
  - name: nayapankh-db
    databaseName: nayapankh
    user: nayapankh_user
    plan: free

services:
  - type: web
    name: nayapankh-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: node server.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
      - key: DATABASE_URL
        fromDatabase:
          name: nayapankh-db
          property: connectionString
      - key: SESSION_SECRET
        generateValue: true
      - key: FRONTEND_URL
        sync: false   # fill in after frontend deploys

  - type: web
    name: nayapankh-frontend
    runtime: node
    rootDir: frontend
    buildCommand: npm install
    startCommand: node server.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
      - key: SESSION_SECRET
        generateValue: true
      - key: API_KEY
        value: vms_live_demo0000
      - key: API_URL
        sync: false   # fill in after backend deploys
