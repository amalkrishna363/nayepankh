<%- include('_layout_start', {
  title: 'Edit ' + v.name, page: 'dashboard',
  breadcrumb: '<a href="/admin/dashboard">Dashboard</a> <span class="bc-sep">›</span> <a href="/admin/volunteer/' + v.id + '">' + v.name + '</a> <span class="bc-sep">›</span> <span>Edit</span>'
}) %>

<div class="admin-page-header">
  <h1 class="admin-page-title">Edit Volunteer</h1>
</div>

<% if(error){ %>
<div class="alert alert-error" style="max-width:820px;margin-bottom:1.2rem">❌ <%= error %></div>
<% } %>

<div class="form-card" style="max-width:820px">
  <form method="POST" action="/admin/volunteer/<%= v.id %>/edit" class="form">

    <div class="form-section">
      <div class="form-section-title"><span class="fsnum">1</span> Personal Information</div>
      <div class="form-row">
        <div class="form-group">
          <label>Full Name <span class="req">*</span></label>
          <input type="text" name="name" value="<%= v.name %>" required>
        </div>
        <div class="form-group">
          <label>Gender</label>
          <select name="gender">
            <option value="">Prefer not to say</option>
            <% ['Male','Female','Other'].forEach(g => { %>
              <option <%= v.gender===g?'selected':'' %>><%= g %></option>
            <% }) %>
          </select>
        </div>
      </div>
      <div class="form-row">
        <div class="form-group">
          <label>Email <span class="req">*</span></label>
          <input type="email" name="email" value="<%= v.email %>" required>
        </div>
        <div class="form-group">
          <label>Phone <span class="req">*</span></label>
          <input type="tel" name="phone" value="<%= v.phone %>" required>
        </div>
      </div>
      <div class="form-row">
        <div class="form-group">
          <label>Age <span class="req">*</span></label>
          <input type="number" name="age" min="14" max="100" value="<%= v.age %>" required>
        </div>
        <div class="form-group">
          <label>Status</label>
          <select name="status">
            <% ['Pending','Active','Inactive'].forEach(s => { %>
              <option <%= v.status===s?'selected':'' %>><%= s %></option>
            <% }) %>
          </select>
        </div>
      </div>
      <div class="form-group">
        <label>Address</label>
        <input type="text" name="address" value="<%= v.address %>">
      </div>
    </div>

    <div class="form-section">
      <div class="form-section-title"><span class="fsnum">2</span> Volunteer Details</div>
      <div class="form-row">
        <div class="form-group">
          <label>Skills</label>
          <input type="text" name="skills" value="<%= v.skills %>">
        </div>
        <div class="form-group">
          <label>Availability</label>
          <select name="availability">
            <option value="">Select…</option>
            <% ['Weekdays','Weekends','Both','Full-time'].forEach(a => { %>
              <option <%= v.availability===a?'selected':'' %>><%= a %></option>
            <% }) %>
          </select>
        </div>
      </div>
      <div class="form-group">
        <label>Emergency Contact</label>
        <input type="text" name="emergency_contact" value="<%= v.emergency_contact %>">
      </div>
      <div class="form-group">
        <label>Admin Notes</label>
        <textarea name="notes" rows="3" placeholder="Internal notes visible only to admins…"><%= v.notes %></textarea>
      </div>
    </div>

    <div class="btn-group">
      <button type="submit" class="btn btn-primary">Save Changes</button>
      <a href="/admin/volunteer/<%= v.id %>" class="btn btn-ghost">Cancel</a>
    </div>
  </form>
</div>

<%- include('_layout_end') %>
